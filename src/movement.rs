/// The representation of each of game movement.
pub enum Move {
    /// Shift up.
    Up,
    /// Shift down.
    Down,
    /// Shift left.
    Left,
    /// Shift right.
    Right,
    /// Undo move.
    Undo,
}
